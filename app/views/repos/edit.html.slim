/ repos/edit
ruby:
  @repo = @repo.decorate

= render @repo

.spacer.is_small

= render 'form'

.spacer.is_extra_tall

- if policy(@repo).destroy?
  .box.is_danger
    .title Danger Zone
    = link_to @repo, method: :delete, data: { confirm: 'Are you sure?' } do
      = icon :destroy, 'Remove Repo from Plugingeek'

