/ repos/index
- cache [@language, @repos] do
  #js-repos-list
    .find-repos
      / Search and sort live in list.js
      input.search type='text' autocomplete='off' placeholder='Authentication...'
      = render 'repos/sort_repos'

    .list data-page=5
      = render partial: 'shared/repo', collection: @repos

    .show-all.js-show-all More repos
