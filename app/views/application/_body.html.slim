/ body.html.slim

/ topbar
.container#topbar_o
  .row
    .twelve.columns#topbar_i

        = render 'topbar'

/ explore bar
.container#explore_bar_o
  .row
    .twelve.columns#explore_bar_i

        = render 'explore_bar'

/ main and sidebar
.container#main_o
  .row

    / main
    .twelve.columns#main_i

      / flash messages
      = render 'flash_messages'

      / main body
      = yield

    / sidebar
  /
    .four.columns#sidebar_i
      .well style='margin-top: 5px'

        markdown:
          **Github** is the home of many outstanding ruby gems. **Knight.io** assists developers in finding and discovering these treasures. **Contribute** your knowledge!

/ footer

/ javascripts in body
= render 'javascripts_body'